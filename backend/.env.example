# API
API_BASE_URL=http://localhost:8000

# Phase 4 — AI Inspection Assistant (optional Ollama)
AI_INSPECTOR_ENABLED=false
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b-instruct

# Phase 6 — Optional vehicle part detection model (local weights)
USE_VEHICLE_PART_MODEL=true
VEHICLE_PART_MODEL_PATH=backend/model/vehicle_parts.pt
